{
	"name": "@Bettencourt-POS/desktop",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "tauri dev",
		"build": "echo 'Desktop builds run in CI (Windows runner). Use: cd apps/desktop && bunx tauri build'",
		"tauri": "tauri"
	},
	"devDependencies": {
		"@tauri-apps/cli": "^2"
	}
}
